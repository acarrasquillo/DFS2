MySQL support for the DFS project

0.  Install mysql server and mysql python

1. Connect to mysql database ;
#mysql -u <user> -p 

1.1 
mysql> CREATE USER 'ram'@'localhost' IDENTIFIED BY 'dfs';
1.2 
mysql> GRANT ALL PRIVILEGES ON * . * TO 'ram'@'localhost';

2. Create database where X in g0X is the group number.
mysql> create database ccom4017_dfs_ram ;

3. Exit the database with CTRL D

3.  Create tables with the folloing shell command:
#mysql -u ram -p < db.sql
